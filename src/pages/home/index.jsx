import React from 'react';
import HeroScene from '@/components/HeroScene';

const Home = () => {
    return (
        <div className="relative w-full">
            <HeroScene />
        </div>
    );
};

export default Home;
